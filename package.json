{
  "name": "match-observable",
  "version": "1.1.0",
  "description": "An helper function to assert if an Observer produces the specified values.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "tsc && jasmine --config=jasmine.json",
    "lint": "tslint -p tsconfig.json --type-check"
  },
  "keywords": [
    "ReactiveX",
    "unit testing",
    "observables"
  ],
  "author": "@akaztp",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/akaztp/match-observable.git"
},
  "peerDependencies": {
    "rxjs": "^5.4.3"
  },
  "devDependencies": {
    "@types/jasmine": "^2.6.0",
    "jasmine": "^2.8.0",
    "rxjs": "^5.4.3",
    "tslint": "^5.7.0",
    "typescript": "^2.3.4"
  }
}
